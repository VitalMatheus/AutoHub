# Use global User identities

Each email identifies one User across the platform, and a User belongs to at most one Organization; Super Admin users have no Organization. This keeps email-and-password login unambiguous and the MVP authorization model simple, accepting that multi-organization membership would require a later model migration.
