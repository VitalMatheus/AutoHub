import { useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { listCustomers } from '@/features/customers/api/customers-api';
import { listVehicles } from '@/features/vehicles/api/vehicles-api';
import { BackLink, CustomerVehicleFields, inputClass, ItemEditor, ItemsTable, money, Pager, Status } from '@/features/shared/document-components';
import { addQuoteItem, createQuote, getQuote, listQuotes, quoteAction, removeQuoteItem, updateQuoteItem } from '../api/quotes-api';
import { convertQuote } from '@/features/work-orders/api/work-orders-api';

const statuses = ['DRAFT', 'PENDING', 'APPROVED', 'REJECTED', 'CANCELLED'];
function Options() { const customers = useQuery({ queryKey: ['customers', 'document-options'], queryFn: () => listCustomers({ page: 1, pageSize: 100, active: true, sort: 'name', direction: 'asc' }) }); const vehicles = useQuery({ queryKey: ['vehicles', 'document-options'], queryFn: () => listVehicles({ page: 1, pageSize: 100, active: true, sort: 'plate', direction: 'asc' }) }); return { customers: customers.data?.data ?? [], vehicles: vehicles.data?.data ?? [], loading: customers.isPending || vehicles.isPending, error: customers.isError || vehicles.isError }; }
export function QuotesListPage() { const [page, setPage] = useState(1); const [status, setStatus] = useState(''); const query = useQuery({ queryKey: ['quotes', page, status], queryFn: () => listQuotes({ page, pageSize: 20, ...(status ? { status } : {}) }), placeholderData: (p) => p }); const rows = query.data?.data ?? []; return <><div className="mb-6 flex items-start justify-between"><div><h1 className="text-2xl font-bold">Orçamentos</h1><p className="mt-1 text-sm text-slate-500">Acompanhe propostas e aprovações.</p></div><Link to="new" className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">+ Novo orçamento</Link></div><section className="space-y-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><select aria-label="Filtrar por status" className={`${inputClass} max-w-xs`} value={status} onChange={(e) => { setStatus(e.target.value); setPage(1); }}><option value="">Todos os status</option>{statuses.map((s) => <option key={s} value={s}>{s}</option>)}</select>{query.isPending ? <div role="status">Carregando orçamentos…</div> : query.isError ? <div role="alert">Não foi possível carregar os orçamentos.</div> : rows.length === 0 ? <p className="py-12 text-center text-slate-500">Nenhum orçamento encontrado.</p> : <><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-500"><tr><th className="px-2 py-3">Número</th><th className="px-2 py-3">Status</th><th className="px-2 py-3">Itens</th><th className="px-2 py-3">Total</th><th /></tr></thead><tbody className="divide-y">{rows.map((q) => <tr key={q.id}><td className="px-2 py-4 font-semibold">#{q.number}</td><td className="px-2 py-4"><Status value={q.status} /></td><td className="px-2 py-4">{q.items.length}</td><td className="px-2 py-4 font-semibold">{money(q.total)}</td><td className="px-2 py-4 text-right"><Link className="font-semibold text-blue-700" to={q.id}>Visualizar</Link></td></tr>)}</tbody></table></div><Pager page={page} totalPages={query.data?.meta.totalPages ?? 1} onPage={setPage} /></>}</section></>; }
export function NewQuotePage() { const navigate = useNavigate(); const { customers, vehicles, loading, error } = Options(); const [customerId, setCustomer] = useState(''); const [vehicleId, setVehicle] = useState(''); const [notes, setNotes] = useState(''); const [message, setMessage] = useState(''); const mutation = useMutation({ mutationFn: () => createQuote({ customerId, vehicleId, ...(notes.trim() ? { notes: notes.trim() } : {}) }), onSuccess: (q) => navigate(`/app/quotes/${q.id}`) }); if (loading) return <div role="status">Carregando opções…</div>; if (error) return <div role="alert">Não foi possível carregar clientes e veículos.</div>; return <div className="mx-auto max-w-3xl"><BackLink to="/app/quotes">Voltar para Orçamentos</BackLink><h1 className="text-2xl font-bold">Novo orçamento</h1><p className="mt-1 text-sm text-slate-500">Crie uma proposta para um cliente e veículo.</p><form onSubmit={(e) => { e.preventDefault(); if (!customerId || !vehicleId) { setMessage('Selecione cliente e veículo.'); return; } setMessage(''); mutation.mutate(); }} className="mt-6 space-y-6 rounded-2xl border bg-white p-6 shadow-sm"><CustomerVehicleFields {...{ customers, vehicles, customerId, vehicleId, setCustomer, setVehicle }} /><label className="text-sm font-medium text-slate-700">Observações<textarea className={`${inputClass} mt-2 min-h-28`} value={notes} onChange={(e) => setNotes(e.target.value)} /></label>{(message || mutation.isError) && <p role="alert" className="text-sm text-red-600">{message || 'Não foi possível criar o orçamento.'}</p>}<div className="flex justify-end gap-3"><Link to="/app/quotes" className="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancelar</Link><button disabled={mutation.isPending} className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">{mutation.isPending ? 'Salvando…' : 'Salvar orçamento'}</button></div></form></div>; }
export function QuoteDetailPage() { const { id = '' } = useParams(); const navigate = useNavigate(); const client = useQueryClient(); const query = useQuery({ queryKey: ['quote', id], queryFn: () => getQuote(id) }); const [feedback, setFeedback] = useState(''); const action = useMutation({ mutationFn: (name: 'submit' | 'approve' | 'reject' | 'cancel') => quoteAction(id, name), onSuccess: (q) => { setFeedback(`Orçamento atualizado para ${q.status}.`); void client.invalidateQueries({ queryKey: ['quote', id] }); void client.invalidateQueries({ queryKey: ['quotes'] }); }, onError: () => setFeedback('Não foi possível alterar o status do orçamento.') }); const add = useMutation({ mutationFn: (input: Parameters<typeof addQuoteItem>[1]) => addQuoteItem(id, input), onSuccess: () => void client.invalidateQueries({ queryKey: ['quote', id] }) }); const remove = useMutation({ mutationFn: (itemId: string) => removeQuoteItem(id, itemId), onSuccess: () => void client.invalidateQueries({ queryKey: ['quote', id] }) }); const update = useMutation({ mutationFn: ({ item, input }: { item: any; input: any }) => updateQuoteItem(id, item.id, input), onSuccess: () => void client.invalidateQueries({ queryKey: ['quote', id] }) }); const conversion = useMutation({ mutationFn: () => convertQuote(id), onSuccess: (wo) => navigate(`/app/work-orders/${wo.id}`, { state: { saved: 'Orçamento convertido em ordem de serviço.' } }), onError: () => setFeedback('Não foi possível converter o orçamento em ordem de serviço.') }); if (query.isPending) return <div role="status">Carregando orçamento…</div>; if (query.isError || !query.data) return <div role="alert">Orçamento não encontrado.</div>; const q = query.data; const editable = q.status === 'DRAFT'; return <><BackLink to="/app/quotes">Voltar para Orçamentos</BackLink><div className="mb-6 flex items-start justify-between gap-4"><div><h1 className="text-2xl font-bold">Orçamento #{q.number}</h1><p className="mt-2"><Status value={q.status} /></p></div><div className="flex flex-wrap gap-2">{q.status === 'DRAFT' && <button onClick={() => action.mutate('submit')} className="rounded-xl bg-blue-600 px-3 py-2 text-sm font-semibold text-white">Enviar para aprovação</button>}{q.status === 'PENDING' && <><button onClick={() => action.mutate('approve')} className="rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white">Aprovar</button><button onClick={() => action.mutate('reject')} className="rounded-xl border px-3 py-2 text-sm font-semibold">Rejeitar</button></>}{q.status === 'APPROVED' && <button onClick={() => conversion.mutate()} className="rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white">Gerar ordem de serviço</button>}{['DRAFT', 'PENDING'].includes(q.status) && <button onClick={() => action.mutate('cancel')} className="rounded-xl border border-red-200 px-3 py-2 text-sm font-semibold text-red-700">Cancelar</button>}</div></div>{feedback && <p role="status" className="mb-5 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{feedback}</p>}<div className="space-y-5"><section className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">Itens do orçamento</h2><ItemsTable items={q.items} editable={editable} onUpdate={(item, input) => update.mutateAsync({ item, input })} onRemove={(itemId) => remove.mutateAsync(itemId)} /><div className="mt-5 border-t pt-4 text-right text-lg font-bold">Total: {money(q.total)}</div></section>{editable && <ItemEditor onAdd={(input) => add.mutateAsync(input)} submitting={add.isPending} />}<section className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">Observações</h2><p className="mt-3 whitespace-pre-wrap text-sm text-slate-600">{q.notes || 'Nenhuma observação.'}</p></section></div></>; }
